<<<<<<< Updated upstream
---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Testing Firebase">
    <main class="flex-container">
        <div id="middle" class="centered-content">
            <h1 class="text-white text-center mt-20" style="font-size: 25px; font-family: Maharlika;">Testing</h1>
            <button id="testButton" class="bg-yellow-500 hover:bg-gray-500 text-white font-bold py-2 px-4 rounded mt-4" style="font-size: 25px; font-family: Maharlika;"> Test </button>
            <button id="addButton" class="bg-yellow-500 hover:bg-gray-500 text-white font-bold py-2 px-4 rounded mt-4" style="font-size: 25px; font-family: Maharlika;"> Add to DB </button>
            <button id="readButton" class="bg-yellow-500 hover:bg-gray-500 text-white font-bold py-2 px-4 rounded mt-4" style="font-size: 25px; font-family: Maharlika;"> Read from DB </button>
        </div>

        

        <script>
            import { testClick } from "../scripts/test.js"
            import { addToDB } from "../scripts/addData.js"
            import { readFromDB } from "../scripts/readData.js"

            document.getElementById('testButton').addEventListener('click', () => {
                testClick();
            });
            document.getElementById('addButton').addEventListener('click', () => {
                addToDB();
            });
            document.getElementById('readButton').addEventListener('click', () => {
                readFromDB();
            });
        </script>
    </main>
</Layout>

<style>
    .flex-container {
        display: flex;
        justify-content: center;
        align-items: center;
    }
    .centered-content {
        text-align: center;
    }
</style>



=======
<!-- ---
import GroupInfo from "../components/Group_info.astro";
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Tarot Cards</title>
	</head>
	<body>
		<h1>Tarot Cards and Values</h1>
		<GroupInfo
			image="/images/group_images/Original_Hedgehog.png"
			groupName="The Devil"
			CardNumber={"12 Cards"}
		/>
		<ul id="tarotList"></ul>

		<script>
			// Assuming readGroup returns a Promise
			import { readGroup } from "../scripts/getGroup.js";

			// Define an async function to use await
			async function fetchData() {
				const groupDict = await readGroup();
				populateHTML(groupDict);
			}

			// Function to populate HTML with data
			function populateHTML(groupDict) {
				const ul = document.getElementById("tarotList");
				ul.innerHTML = ""; // Clear existing content

				// Loop through groupDict and create list items
				for (const card in groupDict) {
					if (groupDict.hasOwnProperty(card)) {
						const value = groupDict[card];
						const li = document.createElement("li");
						li.textContent = `${card}: ${value}`;
						ul.appendChild(li);
					}
				}
			}
			// Call the async function to fetch data and populate HTML
			fetchData();
		</script>
	</body>
</html>
-->
>>>>>>> Stashed changes
